// Dawnlight 2 — 2026-05-08 기준 전체 공개. 모든 사용자 / 모든 페이지 dl2.
// hook 자체는 호환성을 위해 남기되 항상 true 반환. 호출 측의 cosmic 분기는
// dead code 로 남아 있고 점진적으로 제거 중.
export function useDawnlight2(): boolean {
  return true;
}
